services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: market
      POSTGRES_USER: market
      POSTGRES_PASSWORD: market
    ports:
      - "5432:5432"
    volumes:
      - market-postgres:/var/lib/postgresql/data

volumes:
  market-postgres:
